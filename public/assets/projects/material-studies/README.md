# Placeholder asset folder

`Material Studies No. 01` uses CSS-rendered sample artwork so it is impossible
to mistake the example for Brianna's real work.

When replacing this example, place original image files in a sibling folder
named after the real project's slug and reference them as:

`/assets/projects/<project-slug>/<filename>`
